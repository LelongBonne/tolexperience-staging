---
title: "Éducation — TOLExperience (staging)"
layout: "layouts/base.njk"
---

<h1 class="tol-title">Éducation (CAP / Bac Pro)</h1>
<p class="tol-text">Contenus à venir.</p>

<section class="grid grid--home">
  <div class="card"><div class="card-body">
      <h3 class="tol-title">Exemples à venir</h3>
      <p class="tol-text">Cours, feuilles d’exercices et évaluations.</p>
      <a href="{{ '/' | url }}" class="btn btn--primary">Retour accueil</a></div>
  </div>
</section>

<style>
/* Patch local identique pour l’affichage */
.viewport { z-index: 1200 !important; }

section.grid {
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: var(--gap, 16px);
}

.card { position: relative; }
.card::before { z-index: 0; }
.card-body {
  position: relative; z-index: 1;
  padding: 1.5rem;
  display: flex; flex-direction: column; gap: .6rem;
}

.container { color: var(--text-color); }
</style>
