---
title: "Vidéos — TOLExperience (staging)"
layout: "layouts/base.njk"
---
<h1>Vidéos</h1>
<p>Intégration YouTube en mode « privacy enhanced ».</p>

<div class="video-grid">
  {% for id in videos.ids %}
    {% set vid = id %}  {# IDs bruts, pas de filtre #}
    {% if vid %}
    <figure class="video-card">
      <div class="video-frame">
        <iframe
          loading="lazy"
          src="https://www.youtube-nocookie.com/embed/{{ vid }}"
          title="Vidéo {{ loop.index }}"
          allowfullscreen
          style="width:100%;height:100%;border:0"></iframe>
      </div>
    </figure>
    {% endif %}
  {% endfor %}
</div>

<style>
.video-grid{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(280px,1fr));
  gap:16px; margin-top:12px;
}
.video-card{margin:0}
.video-frame{
  position:relative; padding-top:56.25%;
  background:#0f1113; border:1px solid #222; border-radius:8px; overflow:hidden;
}
.video-frame iframe{ position:absolute; inset:0; width:100%; height:100%; border:0; }
</style>
