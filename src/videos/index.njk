---
title: "Vidéos — TOLExperience (staging)"
layout: "layouts/base.njk"
---

<h1 class="tol-btitle">VIDEOS</h1>
<p class="tol-text">Intégration YouTube en mode « privacy enhanced ».</p>

<div class="video-grid">
  {% for id in videos.ids %}
    {% set vid = id %}
    {% if vid %}
    <figure class="video-card">
      <div class="video-frame">
        <iframe
          loading="lazy"
          src="https://www.youtube-nocookie.com/embed/{{ vid }}"
          title="Vidéo {{ loop.index }}"
          allowfullscreen></iframe>
      </div>
    </figure>
    {% endif %}
  {% endfor %}
</div>
